Python script connects to an existing MYSQL database and adds a username and password record.

Password is encrypted with cryptography module and then stored in the database

When username is provided, the encrypted password is fetched and decrypted to the original password 

Enter db user and password
